<%= simple_form_for(@show) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :date %>
    <%= f.association :venue %>
    <%= f.input :description %>
    <%= f.input :img %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
